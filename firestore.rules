rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Todo el acceso se realiza vía Admin SDK (backend), que ignora estas reglas.
    // Se niega cualquier acceso directo de clientes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
