services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: ciudadano
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ciudadano"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
